<%- include ../partials/header.ejs %>

<!-- Content for the Add_Edit page -->
<div class="container">
    <div class="row">
        <div class="offset-md-3 col-md-6">
            <br>
            <h1><%= title %></h1>

            <form class="form" method="POST">
                <div class="form-group">
                    <input type="hidden"
                        name="id"
                        value="<%= item._id %>"
                    >
                    <label for="itemTextField">Contact Name</label>
                    <input type="text" class="form-control"
                        id="itemTextField"
                        placeholder="Enter the Contact Name"
                        name="item" 
                        value="<%= item.item %>"
                        required>
                </div>
                <br />
                <div class="form-group">
                    <label for="QtyTextField">Contact Number</label>
                    <input type="number" class="form-control"
                    id="QtyTextField"
                    placeholder="Enter the Contact Number"
                    name="qty" 
                    value="<%= item.qty %>"
                    required>
                </div>
                <br />
                <div class="form-group">
                    <label for="statusTextField">Email Address</label>
                    <input type="text" class="form-control"
                    id="statusTextField"
                    placeholder="Enter an Email Address"
                    name="status" 
                    value="<%= item.status %>"
                    >
                </div>
                <br />
                <!-- <div class="card">
                    <div class="card-header">Size</div>
                    <div class="card-body">
                        <div class="form-group">
                            <label for="hightTextField">Hight</label>
                            <input type="number" step="0.01" 
                            class="form-control"
                            id="hightTextField"
                            placeholder="0.00"
                            name="size_h" 
                            value="<%= item.size.h %>"
                            required>
                        </div>

                        <div class="form-group">
                            <label for="widthTextField">Width</label>
                            <input type="number" step="0.01"
                            class="form-control"
                            id="widthTextField"
                            placeholder="0.00"
                            name="size_w" 
                            value="<%= item.size.w %>"
                            required>
                        </div>

                        <div class="form-group">
                            <label for="uomTextField">Hight</label>
                            <input type="text" class="form-control"
                            id="uomTextField"
                            placeholder="cm"
                            name="size_uom" 
                            value="<%= item.size.uom %>"
                            required>
                        </div>
                    </div>                    
                </div>                 -->
                <div class="form-group">
                    <!-- <span class="text-muted">[use , to separate tags]</span> -->
                    <label for="tagTextArea">Membership </label>
                    <textarea type="text" class="form-control"
                    id="tagTextArea"
                    placeholder="Enter the membership type of this person"
                    name="tags" 
                    required>
                    <%= item.tags %>
                    </textarea>
                </div>
                <br/>

                <button class="btn btn-primary" type="submit">
                    <% if(buttonText == "Add"){ %>
                        <i class="fa-solid fa-user-plus"></i>
                    <% } else { %>
                        <i class="fa-regular fa-pen-to-square"></i> 
                        <% } %>
                    <%= buttonText %>
                </button>
                
                <a href="/inventory/list" class="btn btn-warning">
                    <i class="fas fa-undo"></i> 
                    Cancel
                </a>

            </form>
        </div>

    </div>
</div>

<%- include ../partials/footer.ejs %>